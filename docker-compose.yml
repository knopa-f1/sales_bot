version: "3.9"

services:
  bot:
    build: .
    env_file:
      - .env
    restart: always
    volumes:
      - .:/app
      - bot_admin_media_data:/media

networks:
  default:
    external:
      name: backend

volumes:
  bot_admin_media_data:
    external: true